.single-comment.micro-blog
  .comment-top
    .username
      = link_to 'Местный тролль', '/'
  .avatar
    = image_tag('/trollface.png', class: 'img-circle')
  .bubble-comment
    .content
      Загружать возможно только картинки или gif-анимацию до 1МБ.
- if user_signed_in?
  -if @image.errors.any? 
    = @image.errors[:image].join
  = form_for @image do |f|
    .upload-image
      %p
        = f.file_field :image
      %p
        = f.submit 'Добавить', class: 'btn btn-primary btn-sm'
- else
  %h1.center Сначала нужно войти