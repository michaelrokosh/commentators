= div_for(comment) do
  .row
    .thumb-image.col-md-4.col-xs-4
      = link_to(image_tag(comment.image.image.url(:thumb)), image_path(comment.image))
    .single-comment.no-left-padding.col-md-8.col-xs-8
      .bubble-comment.bubble-comment-user
        .content
          = comment.content
      .voted
        .rating
          = comment.rating