$('#comments').append('<%= j render partial: 'comments/comment_mobile', collection: @comments, as: :comment %>');
<% if @comments.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate @comments %>');
    $('.pagination').hide();
<% else %>
    $('.pagination, #load_more_comments').remove();
<% end %>