%header
  %navigation
    %ul.menu
      %li#pac
        %a.home{href: "/", title: "Главная"}
      .rmenu
        %li
          %a{href: "/dniwe"} Dniwe
        %li
          %a{href: "/best"} Перлы
        %li
          - if user_signed_in?
            %a.fa.fa-sign-out
              = link_to '', destroy_user_session_path, method: :delete, title: 'Выход'
          - else
            %a.fa.fa-sign-in
              = link_to '', user_omniauth_authorize_path(:vkontakte), title: 'Вход'