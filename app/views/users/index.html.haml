.single-comment.micro-blog
  .comment-top
    .username
      = link_to 'Местный тролль', '/'
  .avatar
    = image_tag('/trollface.png', class: 'img-circle')
  .bubble-comment
    .content
      Местное население. Мирное и веселое, но черезчур переборчивое >_<
.users
  %table.table.table-striped
    %tr
      %td.single-user
      %td.karma-table 
        Карма
    - @users.each do |user|
      %tr
        %td.single-user
          = image_tag(user.avatar_url, class: 'img-circle')
          = link_to user.username, user_path(user)
        %td.karma-table 
          = user.karma